-- 在 ds1 数据库中创建表
CREATE TABLE t_user_0 (
    id BIGINT NOT NULL,
    name VARCHAR(255),
    country VARCHAR(255),
    PRIMARY KEY (id)
);

CREATE TABLE t_user_1 (
    id BIGINT NOT NULL,
    name VARCHAR(255),
    country VARCHAR(255),
    PRIMARY KEY (id)
);

CREATE TABLE t_user_2 (
    id BIGINT NOT NULL,
    name VARCHAR(255),
    country VARCHAR(255),
    PRIMARY KEY (id)
);

